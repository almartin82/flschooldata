% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_assessment.R
\name{fetch_assessment_multi}
\alias{fetch_assessment_multi}
\title{Fetch assessment data for multiple years}
\usage{
fetch_assessment_multi(
  end_years,
  subject = "ela",
  grade = NULL,
  level = "district",
  tidy = TRUE,
  use_cache = TRUE
)
}
\arguments{
\item{end_years}{Vector of school year ends (e.g., c(2022, 2023, 2024))}

\item{subject}{Subject area: "ela" (default) or "math"}

\item{grade}{Grade level (3-10 for ELA, 3-8 for Math). NULL fetches all grades.}

\item{level}{Aggregation level: "district" (default) or "school"}

\item{tidy}{If TRUE (default), returns data in long (tidy) format.}

\item{use_cache}{If TRUE (default), uses locally cached data when available.}
}
\value{
Combined data frame with assessment data for all requested years
}
\description{
Downloads and combines assessment data for multiple school years.
}
\examples{
\dontrun{
# Get 3 years of ELA data
assess_multi <- fetch_assessment_multi(c(2022, 2023, 2024), "ela")

# Track proficiency trends at state level
library(dplyr)
assess_multi |>
  filter(is_state, is_proficient, grade == "03") |>
  group_by(end_year) |>
  summarize(pct_proficient = sum(pct))
}
}
