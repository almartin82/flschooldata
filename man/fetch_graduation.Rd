% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_graduation.R
\name{fetch_graduation}
\alias{fetch_graduation}
\title{Fetch Florida graduation rate data}
\usage{
fetch_graduation(end_year, level = "all", tidy = TRUE, use_cache = FALSE)
}
\arguments{
\item{end_year}{School year end (e.g., 2024 for 2023-24 school year).
Valid years: 2016-2024. See get_available_grad_years().}

\item{level}{One of "state", "district", "school", or "all" (default).
Controls which geographic levels to return.}

\item{tidy}{If TRUE (default), returns long format (one row per school-subgroup).
If FALSE, returns wide format (one row per school, subgroup columns).}

\item{use_cache}{If TRUE, uses cached data if available. Default is FALSE.}
}
\value{
Data frame with graduation rate data.
}
\description{
Downloads and processes 4-year cohort graduation rate data from the Florida
Department of Education (FLDOE).
}
\examples{
\dontrun{
# Get all 2024 graduation data in tidy format
grad_2024 <- fetch_graduation(2024)

# Get state-level only
grad_state <- fetch_graduation(2024, level = "state")

# Get district-level in wide format
grad_district_wide <- fetch_graduation(2024, level = "district", tidy = FALSE)

# See available years
get_available_grad_years()
}
}
