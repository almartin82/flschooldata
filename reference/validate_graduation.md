# Validate processed graduation data

Performs data quality checks on processed graduation data.

## Usage

``` r
validate_graduation(data)
```

## Arguments

- data:

  Processed data frame from process_graduation()

## Value

Invisible TRUE if validation passes, otherwise throws error
